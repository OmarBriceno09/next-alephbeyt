import script from './script-schema';
import letter from './letter-schema';
import presImage from './presImage-schema';
import letterStats from './letterStats-schema';

const schemas = [script, letter, presImage, letterStats];

export default schemas;